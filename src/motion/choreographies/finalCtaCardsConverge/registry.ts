import { finalCtaCardsConvergeTracks } from "./tracks";
export const finalCtaCardsConverge = { choreographyId: "finalCtaCardsConverge", shotId: "shot_30", sceneType: "finalCTA", approved: false, allowedInFactory: false, durationSeconds: 4.4, animationTracks: finalCtaCardsConvergeTracks } as const;
export type FinalCtaCardsConvergeChoreography = typeof finalCtaCardsConverge;
