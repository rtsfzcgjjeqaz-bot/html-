export const config = {
  layoutMode: "modular_dashboard",
  cameraMode: "locked_wide",
  motionStyle: "panel_build",
  density: "high",
  transitionType: "panel_swap",
  depthEnabled: false,
} as const;
