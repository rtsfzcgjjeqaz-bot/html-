export const config = {
  layoutMode: "spatial_layers",
  cameraMode: "orbit_depth",
  motionStyle: "depth_float",
  density: "medium",
  transitionType: "zoom_tunnel",
  depthEnabled: true,
} as const;
