export const config = {
  layoutMode: "decision_flow",
  cameraMode: "analysis_push",
  motionStyle: "scan_step",
  density: "medium",
  transitionType: "logic_morph",
  depthEnabled: false,
} as const;
