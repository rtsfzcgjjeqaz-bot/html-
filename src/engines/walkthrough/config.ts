export const config = {
  layoutMode: "guided_steps",
  cameraMode: "step_focus",
  motionStyle: "cursor_guided",
  density: "medium",
  transitionType: "step_slide",
  depthEnabled: false,
} as const;
