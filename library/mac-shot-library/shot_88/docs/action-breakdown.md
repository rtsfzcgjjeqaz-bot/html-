# Shot 88 Action Breakdown

- shotId: shot_88
- choreographyId: aiPromptCardStackHook
- libraryId: ai-prompt-card-stack-hook
- sceneType: coverHook
- sourceReferencePath: references/extracted-shots/new-reference-ai-promo-ui/shot_88_coverHook_1p53-3p70.mp4
- duration: 114 frames at 30fps

## Visual Structure

Shot 88 introduces the product promise through a dark, cinematic AI prompt stage. A large `Kitaabh's Ai Prompt` headline sits above a layered stack of prompt/task cards. The stack now uses a denser six-card sequence so the motion feels closer to the reference video's cascading AI task deck. The foremost card is bright blue and reads like the active AI action, while supporting cards sit behind it to imply a repeatable workflow.

## Motion Structure

1. Background glow resolves behind the headline.
2. Product headline fades and slides into a stable top-center position.
3. Six task cards rise from depth with staggered parallax.
4. Foreground action card pushes forward after the rear cards connect into one smooth stack.
5. Prompt strip appears near the bottom edge of the stack.
6. Small sparkle accent and card shadows settle.

## Atomic Motion Candidates

- backgroundGlowResolve
- headlineReveal
- staggeredTaskCardCascade
- activePromptCardPush
- promptStripReveal
- hookSoftSettle

## Reuse Value

This is a strong reusable cover hook for AI products, SaaS automation, financial workflow tools, and prompt-based product demos. It is more reusable than the isolated brand intro because the stacked cards communicate the product behavior rather than only brand presence.

## Risks

- The headline must stay short enough to avoid crowding the top safe area.
- Stacked task labels should remain broad and configurable; hard-coded accounting copy would limit reuse.
- Glow and sparkle accents should remain semantic and restrained, not random decoration.
- Subtitle text can become unreadable at smaller output sizes, so it should be optional in future runtime packaging.

## Recommended Action

Proceed as the first certification preview for the new reference queue. Keep it pending review until the user visually approves it.
