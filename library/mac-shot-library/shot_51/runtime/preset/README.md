# Preset Notes

Shot 51 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- warm insight canvas fade
- dashboard card lift
- hero metric stagger
- timeline bar draw
- supporting analysis row resolve
- page stack fan settle
