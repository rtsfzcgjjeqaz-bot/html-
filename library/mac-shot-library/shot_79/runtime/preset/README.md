# Preset Notes

Shot 79 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- selected document context
- toolbar action dock
- edit command tooltip cycle
- suggestion card expand
- suggestion copy resolve
- suggestion focus settle
