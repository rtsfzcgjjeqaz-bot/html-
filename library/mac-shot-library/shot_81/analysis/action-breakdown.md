# Shot 81 Action Breakdown

## Identity

- shotId: shot_81
- choreographyId: darkPromptCodeCanvasPush
- libraryId: dark-prompt-code-canvas-push
- sceneType: searchDemo
- sourceReferencePath: references/extracted-shots/new-reference-ai-interaction/shot_81_searchDemo_12p4-14p45.mp4
- duration: 62 frames at 30fps

## Visual Structure

Shot 81 is a dark product-demo transition that turns a written intent into a coding workspace. The upper copy stays minimal and centered, with the keyword `code` receiving a blue emphasis. The lower left contains a tilted code editor surface that pushes into view from darkness, giving the shot a focused "idea becomes buildable interface" meaning.

## Motion Language

1. Background energy resolves first, using a blue-violet glow behind the text and editor.
2. The top phrase appears softly, then locks focus on the highlighted keyword.
3. A code canvas rises from the lower left with slight perspective and a controlled push-in.
4. Code rows and a selected block reveal in staggered passes.
5. A compact cursor/selection accent lands on the code block.
6. The whole composition settles without drifting decoration.

## Reuse Value

This shot is useful for search-demo, prompt-to-app, AI coding assistant, and feature build-flow videos. It should not be treated as a generic decorative code background; the reusable value is the semantic conversion from idea text to active code canvas.

## Risks

- Highlighted keyword can become too small if the headline is replaced with longer copy.
- Tilted editor must remain inside safe area and cannot crop critical code rows.
- Code rows should read as interface structure, not random decorative stripes.
- The shot is close to other dark AI UI shots, so it should be used when code generation is specifically relevant.

## Recommended Review Action

Preview locally and confirm whether the top keyword highlight plus angled code canvas feels sufficiently distinct from existing prompt-composer and toolbar shots. Keep `approved` and `allowedInFactory` false until visual review passes and GitHub publication is explicitly requested.
