# Preset Notes

Shot 78 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- document surface tilt in
- paragraph rows resolve
- selection highlight sweep
- AI toolbar dock reveal
- command tooltip pop
- selection focus settle
