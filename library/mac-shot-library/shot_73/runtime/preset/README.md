# Preset Notes

Shot 73 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- wide solution board focus
- promise panel slide in
- capability column build
- solution tile highlight sweep
- outcome proof panel push
- bottom summary rail reveal
- matrix camera settle
