# Preset Notes

Shot 64 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- dark card stack bloom
- background card depth drift
- foreground card focus snap
- result label number pop
- short impact hold settle
