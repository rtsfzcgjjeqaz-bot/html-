# Preset Notes

Shot 76 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- dark hero stage resolve
- desktop assistant panel push
- phone panel depth parallax
- assistant greeting reveal
- hero value CTA reveal
- assistant hero camera settle
