# Preset Notes

Shot 50 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- website headline reveal
- dashboard shell slide-in
- KPI card stagger
- heatmap cell paint
- insight module resolve
- module fanout settle
