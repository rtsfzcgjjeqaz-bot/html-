# Preset Notes

Shot 48 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- workflow canvas wash-in
- paired step card reveal
- directional connector draw
- progressive row resolve
- soft camera push settle
