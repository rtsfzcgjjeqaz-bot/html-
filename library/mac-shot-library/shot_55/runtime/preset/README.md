# Preset Notes

Shot 55 currently packages its executable motion inside the choreography component and shot-specific atomic motion file.

Before Windows runtime integration, review whether these motions should be promoted into shared presets:

- AI canvas label reveal
- prompt input slide-in
- typed requirement text
- generation status pulse
- workspace rise and sharpen
- generated row resolve
