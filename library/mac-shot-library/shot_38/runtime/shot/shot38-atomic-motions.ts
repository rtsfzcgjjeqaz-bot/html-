export type AtomicMotion = {
  id: string;
  purpose: string;
  timingFrames: [number, number];
  properties: Record<string, unknown>;
  reviewRisk: string;
};

export const shot38AtomicMotions = [
  {
    id: "headline-highlight-pass",
    purpose: "Establish the mobile AI value statement before the device arrives.",
    timingFrames: [0, 54],
    properties: {
      opacity: [0, 1],
      translateY: [12, 0],
      highlightGradient: ["#5b6df5", "#d94bbf"],
    },
    reviewRisk: "Highlighted wording must remain readable and not overflow in localization.",
  },
  {
    id: "phone-perspective-enter",
    purpose: "Introduce the mobile app as the main reusable stage.",
    timingFrames: [42, 104],
    properties: {
      translateX: [210, 0],
      rotateZ: [-7, -2],
      rotateY: [-16, -7],
      scale: [0.9, 1],
    },
    reviewRisk: "The phone cannot clip outside the safe area or become too skewed for QA.",
  },
  {
    id: "mobile-greeting-focus",
    purpose: "Reveal the app greeting and create a clear text hierarchy.",
    timingFrames: [82, 130],
    properties: {
      opacity: [0, 1],
      translateY: [12, 0],
    },
    reviewRisk: "Small UI text should be suggestive, not relied on as the only meaning.",
  },
  {
    id: "brief-card-expand",
    purpose: "Show the AI recommendation card expanding inside the phone.",
    timingFrames: [110, 166],
    properties: {
      opacity: [0, 1],
      scale: [0.94, 1],
      rowStaggerFrames: 8,
    },
    reviewRisk: "Card expansion must feel generated by the UI, not like a floating sticker.",
  },
  {
    id: "media-module-rise",
    purpose: "Add a mobile content module after the recommendation card.",
    timingFrames: [142, 188],
    properties: {
      opacity: [0, 1],
      translateY: [34, 0],
      cornerRadius: 20,
    },
    reviewRisk: "Media block must be framed as app content and not a random image panel.",
  },
] as const satisfies readonly AtomicMotion[];

export const shot38AtomicMotionIds = shot38AtomicMotions.map((motion) => motion.id);

export const shot38AtomicMotionPackage = {
  shotId: "shot_38",
  libraryId: "mobile-ai-brief-card-reveal",
  choreographyId: "mobileAiBriefCardReveal",
  sceneType: "aiRecommendation",
  status: "preview_ready",
  approved: false,
  allowedInFactory: false,
  implementationVerified: false,
} as const;
